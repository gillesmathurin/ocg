		<% form_for article, :html => {:multipart => true} do |f| -%>
			<%= f.error_messages %>
			
			<p>
				<%= f.label :title, "Titre" %> :<br/>
				<%= f.text_field :title %>
			</p>
			<p>
				<%= f.label :content, "Contenu" %> :<br/>
				<%= f.text_area :content, :rows => 10, :class => "mceEditor" %>
			</p>
			<p>
				<%= f.label :photo, "Image" %> :<br/>
				<%= f.file_field :photo %>
			</p>
			
			<p><%= f.submit "Créer" %></p>
		  
		<% end -%>