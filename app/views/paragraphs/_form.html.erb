		<%= form_for :paragraph, :url => article_paragraphs_path(@article) do |f| -%>
			<% if @paragraph.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@paragraph.errors.count, "error") %> empêchèrent ce paragraph d'être enregistré:</h2>

          <ul>
          <% @paragraph.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
		  <p>
		  	<%= f.label :photo1, "Photo1" %> : <%= f.file_field :photo1 %>
		  </p>
			<p>
				<%= f.label :photo2, "Photo2" %> : <%= f.file_field :photo2 %>
			</p>
			<p>
				<%= f.label :photo3, "Photo3" %> : <%= f.file_field :photo3 %>
			</p>
			<p>
				<%= f.label :content, "Contenu" %> : <br/>
				<%= f.text_area :content, :rows => 25, :cols => 60, :class => "mceEditor" %>
			</p>

			<p><%= f.submit "Enregistrer" %></p>
		<% end -%>